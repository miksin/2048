<div class="playground">(playground)</div>

<style>
	.playground {
		width: var(--playground-size-md);
		height: var(--playground-size-md);

		background-color: var(--base-color-3);
		border-radius: 6px;
	}

	@media screen and (max-width: 512px) {
		.playground {
			width: var(--playground-size-sm);
			height: var(--playground-size-sm);
		}
	}
</style>
